import styled from "styled-components";

export const CustomContainer = styled.div`
  height: calc(100% - 120px);  // Handle top bar which is 60px

`
export const ArticleHeaderContainer = styled(CustomContainer)`
  margin: 100px auto 0;
  min-height: 450px;

`